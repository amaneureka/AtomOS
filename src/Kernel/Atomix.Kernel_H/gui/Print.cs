using System;

using Atomix.Kernel_H.drivers.video;

namespace Atomix.Kernel_H.gui
{
    public static class Print
    {
        /*
        public static void Sprite(UInt32[] BackBuffer, UInt32[] Image, uint x, uint y, uint width, uint height)
        {
            uint p = 0;            
            for (uint j = 0; j < height; j++)
            {
                for (uint i = 0; i < width; i++)
                {
                    BackBuffer[p] = VBE.GetPixel(i+x, j+y);
                    VBE.SetPixel(i+x, j+y, Image[p++]);
                }
            }
        }

        public static void Sprite(UInt32[] Image, uint x, uint y, uint width, uint height)
        {
            uint p = 0;
            for (uint j = 0; j < height; j++)
            {
                for (uint i = 0; i < width; i++)
                {
                    VBE.SetPixel(i+x, j+y, Image[p++]);
                }
            }
        }

        public static void GetBuffer(UInt32[] BackBuffer, uint x, uint y, uint width, uint height)
        {
            uint p = 0;
            for (uint j = 0; j < height; j++)
            {
                for (uint i = 0; i < width; i++)
                {
                    BackBuffer[p++] = VBE.GetPixel(i+x, j+y);
                }
            }
        }*/
    }
}
